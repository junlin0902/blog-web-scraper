#!/usr/bin/env node
import { createRequire } from "node:module";
const require2 = createRequire(import.meta.url);
const requireFromJest = createRequire(require2.resolve("jest/package.json"));
const bin = requireFromJest("./package.json")["bin"];
requireFromJest(bin);
