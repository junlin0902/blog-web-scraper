"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var fragmentsHandler_exports = {};
__export(fragmentsHandler_exports, {
  command: () => command,
  description: () => description,
  handler: () => handler
});
module.exports = __toCommonJS(fragmentsHandler_exports);
var import_node_fs = __toESM(require("node:fs"));
var import_node_path = __toESM(require("node:path"));
var import_execa = __toESM(require("execa"));
var import_listr2 = require("listr2");
var import_prettier = require("prettier");
var import_cli_helpers = require("@redwoodjs/cli-helpers");
var import_project_config = require("@redwoodjs/project-config");
var import_runTransform = require("../../../../../lib/runTransform");
const command = "fragments";
const description = "Set up Fragments for GraphQL";
async function handler({ force }) {
  const tasks = new import_listr2.Listr(
    [
      {
        title: "Update Redwood Project Configuration to enable GraphQL Fragments",
        skip: () => {
          if (force) {
            return false;
          }
          const config = (0, import_project_config.getConfig)();
          if (config.graphql.fragments) {
            return "GraphQL Fragments are already enabled.";
          }
          return false;
        },
        task: () => {
          (0, import_cli_helpers.setTomlSetting)("graphql", "fragments", true);
        }
      },
      {
        title: "Generate possibleTypes.ts",
        task: () => {
          import_execa.default.commandSync("yarn redwood generate types", { stdio: "ignore" });
        }
      },
      {
        title: "Import possibleTypes in App.tsx",
        task: () => {
          return (0, import_runTransform.runTransform)({
            transformPath: import_node_path.default.join(__dirname, "appImportTransform.js"),
            targetPaths: [(0, import_project_config.getPaths)().web.app]
          });
        }
      },
      {
        title: "Add possibleTypes to the GraphQL cache config",
        task: async () => {
          const transformResult = await (0, import_runTransform.runTransform)({
            transformPath: import_node_path.default.join(__dirname, "appGqlConfigTransform.js"),
            targetPaths: [(0, import_project_config.getPaths)().web.app]
          });
          if (transformResult.error) {
            throw new Error(transformResult.error);
          }
          const appPath = (0, import_project_config.getPaths)().web.app;
          const source = import_node_fs.default.readFileSync(appPath, "utf-8");
          const prettierOptions = await (0, import_cli_helpers.getPrettierOptions)();
          const prettifiedApp = await (0, import_prettier.format)(source, {
            ...prettierOptions,
            parser: "babel-ts"
          });
          import_node_fs.default.writeFileSync((0, import_project_config.getPaths)().web.app, prettifiedApp, "utf-8");
        }
      }
    ],
    { rendererOptions: { collapseSubtasks: false } }
  );
  try {
    await tasks.run();
  } catch (e) {
    console.error(import_cli_helpers.colors.error(e.message));
    process.exit(e?.exitCode || 1);
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  command,
  description,
  handler
});
