"use strict";
var import_vitest = require("vitest");
(0, import_vitest.describe)("Middleware codemod", () => {
  (0, import_vitest.it)("Handles the default TSX case", async () => {
    await matchTransformSnapshot("codemodMiddleware", "defaultTsx");
  });
  (0, import_vitest.it)("Handles when OgImageMiddleware is already imported", async () => {
    await matchTransformSnapshot("codemodMiddleware", "alreadyContainsImport");
  });
  (0, import_vitest.it)("Handles when registerMiddleware function is already defined", async () => {
    await matchTransformSnapshot(
      "codemodMiddleware",
      "registerFunctionAlreadyDefined"
    );
  });
});
