"use strict";
var import_vitest = require("vitest");
(0, import_vitest.describe)("Vite plugin codemod", () => {
  (0, import_vitest.it)("Handles the default vite config case", async () => {
    await matchTransformSnapshot("codemodVitePlugin", "defaultViteConfig");
  });
});
