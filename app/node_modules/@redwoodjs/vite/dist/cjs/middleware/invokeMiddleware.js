"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var invokeMiddleware_exports = {};
__export(invokeMiddleware_exports, {
  invoke: () => invoke
});
module.exports = __toCommonJS(invokeMiddleware_exports);
var import_server_store = require("@redwoodjs/server-store");
var import_middleware = require("@redwoodjs/web/dist/server/middleware");
const invoke = async (req, middleware, options) => {
  const mwReq = new import_middleware.MiddlewareRequest(req);
  if (typeof middleware !== "function") {
    (0, import_server_store.setServerAuthState)(mwReq.serverAuthState.get());
    return [import_middleware.MiddlewareResponse.next(), mwReq.serverAuthState.get()];
  }
  let mwRes = import_middleware.MiddlewareResponse.next();
  try {
    const output = await middleware(mwReq, import_middleware.MiddlewareResponse.next(), options) || import_middleware.MiddlewareResponse.next();
    mwRes = output;
  } catch (e) {
    if (e instanceof import_middleware.MiddlewareShortCircuit) {
      return [e.mwResponse, mwReq.serverAuthState.get()];
    }
    console.error("Error executing middleware > \n");
    console.error("~".repeat(80));
    console.error(e);
    console.error("~".repeat(80));
  } finally {
    (0, import_server_store.setServerAuthState)(mwReq.serverAuthState.get());
  }
  return [mwRes, mwReq.serverAuthState.get()];
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  invoke
});
