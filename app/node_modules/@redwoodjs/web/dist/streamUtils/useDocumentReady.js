import { useEffect } from "react";
const useDocumentReady = (callback) => {
  return useEffect(() => {
    const handler = (_e) => {
      if (document.readyState === "complete") {
        callback();
      }
    };
    document.addEventListener("readystatechange", handler);
    return () => {
      document.removeEventListener("readystatechange", handler);
    };
  }, [callback]);
};
export {
  useDocumentReady
};
